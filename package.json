{
  "name": "svipall-site",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check:links": "node scripts/check-links.mjs",
    "verify": "npm run build && npm run check:links && npm run check:glyphs",
    "check:glyphs": "node scripts/check-glyphs.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "lucide-static": "^1.43.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
